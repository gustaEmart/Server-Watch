{
  "name": "serverwatch-mvp",
  "version": "0.1.0",
  "description": "ServerWatch MVP - monitoramento de disponibilidade via ping em rede local.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "probe:setup": "node probe/setup-server.js",
    "check": "node --check server.js && node --check public/app.js && node --check probe/collector.js && node --check probe/setup-server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
